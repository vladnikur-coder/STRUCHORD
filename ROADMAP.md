# STRUCHORD — ROADMAP (план будущего)

Заведён 2026-08-25 по решениям ask_user:
- формат: **отдельный ROADMAP.md** (README.md остаётся журналом прошлого — только растёт вглубь; этот файл — план, живёт и переписывается);
- приоритеты: предложил ассистент (оценка «размер × польза × риск»), **ждут утверждения пользователя одной правкой**;
- ритм: **поток + фиксация** — идеи приходят на ходу, но каждая сразу встаёт строкой в Backlog, и волна стартует только оттуда.

## Правила

- **R1.** README = прошлое (спеки, прогоны, sha). ROADMAP = будущее (очередь, статусы, реестр). Ничего не дублируем, ссылаемся.
- **R2.** Любая новая идея — сначала строка в Backlog (статус «новая»), потом волна. Волна на старте получает «в работе», на сдаче — «сдано» + sha.
- **R3.** Один фронт: одновременно в работе ровно одна волна.
- **R4.** Отменённое не стираем: статус «отменено» + причина (история решений дороже чистоты списка).
- **R5.** Чистки — отдельные волны, не вперемешку с фичами.
- **R6.** Каждая сдача ритм-линии: обновить реестр (sha/sw) + запись в README (как делаем).
- **R7.** При изменении `STRUCHORD.html` держать видимую версию приложения в шапке (`STRUCHORD · ver 0.xx`) актуальной вместе с `sw.js` / `CACHE_NAME`, чтобы пользователь сразу видел текущий build.

## Рабочий формат с пользователем

Зафиксировано 2026-08-31 по B-31: пользователю нравится формат совместной итерации «обсудили ощущение → узкий прототип → проверка в живом браузере → точечная доработка». Если сессию продолжает другой агент, держать этот формат.

- Говорить по-русски, кратко, но технически точно. Не прятать сомнения: если поведение визуальное — лучше дать проверить в preview, чем обещать на словах.
- Перед реализацией спорной анимации сначала уточнять желаемое **ощущение/картинку**, а не только технический механизм. В B-31 ошибка была в слишком буквальном «overlay/guide» вместо нужного smooth snapped resize.
- Работать малыми шагами: один видимый сценарий за раз. Для B-31 сначала граница между ячейками; правый край квадрата — только после визуальной приёмки принципа.
- Для анимаций держать критерий пользователя: «дорого», плавно, без дёрганья и без ощущения дешёвой заглушки. Если пользователь говорит «было лучше» или «отменяй» — быстро откатить/зафиксировать причину, не защищать неудачный вариант.
- Производительность не опускать: цель 60fps. На `pointermove` избегать полного `render()`, `innerHTML`, тяжёлого reslice/settle и записи модели; допустима только локальная дешёвая геометрия с rAF/throttle.
- Сдача визуальной фичи — только после ручной проверки пользователем в live preview. Тесты обязательны для регрессий, но не заменяют визуальную приёмку.
- **Скриншоты/shot-tools не запускать без необходимости.** Использовать их только если пользователь попросил или визуальный баг нельзя понять иначе; по возможности сначала обсудить, что именно снимаем.

## Решения ask_user (пакет планирования, 2026-08-25)

> 2026-08-28 (дословно): «принцип один файл, без сборки появился из за того, что мне было удобнее так открывать и тестировать. сейчас, когда есть порт 8000 я делаю это прямо в браузере, так что это не проблема» — **принцип «один файл, без сборки, без внешних зависимостей» разморожен**: он был про удобство локального открытия, а не догму. Следствия (НЕ автоматические волны, а снятие запрета): совет про ES-модули и вариант «lit-html/preact» в B-25 перестают быть принципиально запрещёнными; B-24 может рассматривать библиотечный стор. Цена каждого такого шага остаётся и считается на старте конкретной волны: канал распространения (двойной клик/офлайн-PWA у людей без сервера), ритуал sha/sw, тестовая инфраструктура под неоднофайловую раскладку.

| Вопрос | Выбор |
|--------|-------|
| Следующая волна | **B-09** — включаемая возможность редактировать ячейки и ритм в режиме ленты (после сдачи B-15); перед стартом уточнить объём |
| Мобильная стратегия (B-07) | **mob_mode** — мобильный РЕЖИМ в том же файле, отдельной версии не будет |
| Стартовый экран (B-13) | **start_later** — отложить до эскиза-спеки на старте волны (хранение решим там) |
| Слой мелодии (B-12) | **pr_last** — последней фазой, после платформенных блоков |


## Зоны приложения

| Зона | Что это | Критичность |
|------|---------|-------------|
| Ритм («Рулон и окна») | хранилище рулонов, жесты ресайза, наследование (волны 1–7) | продакшн-критична: sha/sw, три бенча |
| Аппликатуры | оценка/каталог аппликатур (волны 2–7), якоря, пресет-лаб | **линия закрыта 2026-08-25** — пользователь: «пока мне все нравится» |
| Редактор | модалки боя/перебора, канвас превью | трогает обе зоны выше |
| PWA / мобильный | sw.js, manifest, мобильный бенч | деплой-ритуал пары файлов |
| Инфра dev | тесты jsdom, зонды, Chrome-бенчи | честность прогонов |
| Архитектура/состояние | store, единый dispatch, подписки рендера (B-24) | основа под undo/redo и отладку, трогает все зоны выше |

## Backlog

Порядок — моё предложение (меньшие и более выпрямляющие вперёд). Утверди одной правкой или назови свой топ — переставлю.

### Активные

| # | ID | Зона | Что (источник — README) | Размер | Польза | Статус |
|---|----|------|--------------------------|--------|--------|--------|
| 1 | B-04 | Редактор | Засечки убраны, подписи «1 та и та» на их месте, номера долей выделены | S | чистый вид сетки долей | **сдано 2026-08-25** |

| 2 | B-05 | Чистка | ~~substep-ветка buildInnerTicks~~ снова жива с B-22 (редактор зовёт её) — выпала; в чистке остаётся только мёртвое правило `.is-count-tight` (класс нигде не ставится); решения: снос целиком с NB-комментарием, полный цикл проверки | XS | код честнее | **сдано 2026-08-27** |
| 3 | B-06 | Ритм | Ресайз: если бой секции не объявлен, а у ячейки оказался пустой бой — он не должен «магически» появляться и показываться кастомным (спека из пачки 2026-08-25) | S–M | логика без сюрпризов | **сдано 2026-08-26** |
| 4 | B-16 | UI | Выбор цветовой схемы при длинном нажатии на кнопку переключения темы | S | приятное, дёшево | **сдано 2026-08-26** |
| 5 | B-15 | Редактор | Подсказка ритма при ресайзе: по умолчанию вид как раньше; при протяжке границы кастомный ритм поударно переплывает из mini-preview к узлам над счётом, наследуемый ритм появляется fade-in после приплывания кастома; на отпускании наследуемый сначала fade-out, затем кастом уплывает обратно. Снятый к финалу ритм не летит, а гаснет; новая кастомная ячейка после resize тоже получает обратный flight; зум/clip/mini-preview handoff закрыты после B-25 | M | читаемость боя при ресайзе | **сдано 2026-08-30** |
| 6 | B-09 | Лента | Включаемая возможность редактировать ячейки и ритм в режиме ленты | M–L | единый рабочий режим | новая |
| 7 | B-08 | UI/полировка | Анимации: аудит сломанных + обрамление топорных действий | M | приложение благовиднее | новая |
| 8 | B-07 | Мобильная | Мобильный РЕЖИМ в том же файле для тач-экранов (некоторые функции неудобны) — решено 2026-08-25: отдельной версии не будет | L | основной сценарий на телефоне | новая |
| 9 | B-13 | PWA/UI | Стартовый экран: создать/загрузить песню, пул песен, настройки — отложить до эскиза-спеки (решение 2026-08-25) | L | вход в приложение | новая |
| 10 | B-11 | Лента/текст | Текст песни: слоги по ритму (удобное раскидывание) или просто слова | L | songwriter-фича | новая |
| 11 | B-10 | Экспорт | Экспорт в GTP | L | интероп с Guitar Pro | новая |
| 12 | B-14 | UI/бренд | Логотип (шапка, manifest, иконки) | XS–S | лицо приложения | новая |
| 13 | B-12 | Мелодия | Второй слой мелодии с редактором (пианоролл/табы как в Guitar Pro) — «очень комплексная задача», фаза: последняя (решение 2026-08-25) | XL | следующая эпоха приложения | новая |
| 14 | B-18 | Ритм/чистка | **чистка-4**: снос фасада «рисунка квадрата» (`sq.strumPattern`): «рисунка квадрата не существует, мы отменили эту концепцию. общий ритм можно задать только для секции, больше никак. если в коде ещё упоминается общий ритм для квадрата, это можно спокойно удалять» (решение 2026-08-26). Миграция старых сейвов (материализовать наследников или удалить поле) — спросить на старте волны | M | модель одноуровневая, код честнее | новая |
| 16 | B-23 | Ритм | «−»: поглотитель с пином циклит СВОЙ рулон на поглощённое время, звук удалённой соседки теряется. Спека (2026-08-27, дословно): «общий бой секции D_DU_UDU; у первой ячейки кастомный бой D_XU_UXU; я нажимаю минус на второй ячейке; у получившейся ячейки должен быть бой D_XU_UXUD_DU_UDU». Дырка B-20: сшивка adoptTape только при `removedRef && !absorbRef`. Решения (2026-08-27): full_record (склеивать звучавшее всегда), stays_facade (две общие — без пина), B-23 в работу вместо B-15; урок зонда: половина без рисунка играет заводской удар — пишется он, не тишина | S | звук не тихничает | **сдано 2026-08-27** |
| 15 | B-01 | Инфра | `dev/bench/mobile.js` FAIL ×3 — починен продуктом (цели ≥40px в мобильном портрете), бенч зелёный | S | честность прогонов | **сдано 2026-08-25** |
| 16 | B-02 | Ритм/чистка | **чистка-2**: мёртвые ветки `scope='square'` в редакторе (фича отменена волной-7) | S–M | код честнее, фасад однозначен | **сдано 2026-08-25** |
| 17 | B-03 | Ритм | Лесенка утончения: пошаговое ×2, минимальная достаточная сетка (sub8 доступна) | M | логичность глубоких зумов | **сдано 2026-08-25** |
| 18 | B-19 | Редактор | Счётные подписи по эпохам размера: ячейка с ДРУГИМ эффективным timesig открывает новую эпоху с «1» и нумерацией по её размеру (пример пользователя: 1и2и|1и2и3и4и — ячейки 2/2 и 4/4); возврат к родительскому размеру = тоже смена эпохи; следующие такты рестартируют каждый такт (решение epoch_all, ask_user 2026-08-26) | S | читаемость при сменах размера | **сдано 2026-08-26** |
| 19 | B-20 | Ритм | Дефект-репро от пользователя (2026-08-26): автонаследование (волна-7) не снимает пин ячейки, когда та входит в ЧЕСТНУЮ СВЯЗКУ («+», рулон общий на несколько ячеек) и её окно совпадает с боем секции — плашка кастома висит. Разобрать: демоция одной ячейки из связки ломает/не ломает ленту, критерий «вся связка == фасад» vs «часть связки» | S–M | доверие к автонаследованию | **сдано 2026-08-26** |
| 20 | B-21 | Редактор/UI | Подписи счёта «1 та и та» поднять чуть выше от нижней кромки (наследовали позицию засечек, но тексту там тесно) + «1» должна выделяться из остальных: НАЙДЕНО — уточнение B-04 недожало CSS (класс `is-downbeat` ставится JS, а стиля для него нет; размер/вес висят на всех цифрах через `.is-beat`) | XS | читаемость сетки | **сдано 2026-08-26** |
| 21 | B-22 | Редактор/UI | Возврат засечек под подписи счёта: «пусть под цифрами все таки будут невысокие засечки. не меняй местоположение цифр, просто верни засечки и сделай их высоту меньше» (спека дословно, 2026-08-26). Слои и геометрия — как до B-04 (buildInnerTicks, эпохи B-19), высоты меньше прежних | XS | сетка читается и по черточкам | **сдано 2026-08-26** |
| 22 | B-24 | Архитектура | Store (заметка пользователя 2026-08-28): состояние размазано по глобальным переменным, мутации напрямую → простой store с иммутабельными обновлениями, все изменения через единый dispatch, рендеринг подписывается на изменения. Упростит отладку и сделает историю дешевле. Уточнение при сверке 2026-08-28: **undo/redo уже есть** (снепшотная история: btnUndo/btnRedo, `historyStack` ~L29620, Ctrl+Z/Ctrl+Shift+Z) — store превратит её из полноснепшотной в командную (дешевле по памяти, точечные шаги); главная польза — единый стык правок и предсказуемость | XL | единый стык правок, отладка, история-команды | новая |
| 23 | B-25 | Архитектура | Инкрементальный рендеринг (заметка пользователя 2026-08-28): любое изменение → `render()` (~L27710) полностью перестраивает DOM. Сданный MVP: локальные sync/rerender для resize, ячеек, квадратов, ритм-модалки, повторов/BPM, header/key/timeSig секции и правок аккорда; rAF-throttle для live resize; полный render оставлен для глобальных/редких операций | L | живость UI на больших песнях, меньше визуальных гонок | **сдано 2026-08-29** |
| 24 | B-26 | Аппликатуры | Кэш аппликатур (заметка пользователя 2026-08-28; сверено: `fingeringCache` L8847, `clear()` в ~13 местах — в т.ч. смена тональности и любая правка пользовательской формы, с комментарием «точечного delete мало: семейные производные»). Решение: шире ключ (аккорд + тональность + контекст: предыдущий аккорд, бас), хранить сразу отсортированные варианты. ⚠ Линия аппликатур закрыта 2026-08-25: контекст в ключе меняет ПОДБОР, не только перформанс — на старте волны решить: чистый перф (поведение бит-в-бит то же) или переоткрытие линии (тогда по правилу архива А-* — перевыпуск) | M | меньше пересчётов, стабильнее подбор | новая |
| 25 | B-27 | Редактор/UI | Дебаунсинг частых событий (заметка пользователя 2026-08-28; сверено: рендер УЖЕ схлопывается — `requestRender()` ставит один rAF-рендер на серию правок, прямых вызовов `render()` нет). Открытое ядро — входная сторона: ввод аккорда и pointermove ресайза на каждое событие делают работу модели до рендера. Кандидаты: debounce на ввод, rAF-троттлинг движений (прецедент есть — `zoomChromeRAF` ~L9313). Пересекается с B-15 (подсказка живёт на драге ресайза) и B-25 | S | меньше лишней работы на событие | новая |
| 26 | B-28 | Редактор/анализ | Автоопределение тональности — каденции (заметка пользователя 2026-08-28; заметка сформулирована как «не учитывает последовательности и длительности», сверено 2026-08-28: длительности, бас со слэш-аккордами, доминантсепты, крайние аккорды и профили Krumhansl–Kessler (1982) УЖЕ в проде — `detectKeyFromChords` ~L15693, `KEY_TEMPLATES` ~L15627). Открытое ядро: учёт ПОСЛЕДОВАТЕЛЬНОСТЕЙ — гармонические каденции (IV–V–I, ii–V–I…), направление разрешений между соседними аккордами | M | точнее авто-ключ на реальных прогрессиях | новая |
| 27 | B-29 | Ритм | Дефект-репро пользователя (2026-08-28/29, дословно): «Сброс по какой то причине не очищает кастомный ритм». Корень: глобальный `clearAll()` чистил `sections` и `nextId`, но оставлял `songRhythmRolls`; после нового `addSection()` id 1/2 переиспользовались, и старый ref `1:2:0` простреливал в новую песню. Исправлено: `resetRhythmStorage()` очищает pool/refs/sectionRolls и preview maps на `clearAll()`; тест `b29-clear-custom-rhythm.js` | S | сброс = честно пустая песня | **сдано 2026-08-29** |
| 28 | B-30 | Ритм | Дефект-репро пользователя (2026-08-28, дословно): «В некоторых ситуациях, когда ритм внутри ячейки не полностью совпадает с общим ритмом секции, ритм все равно не рисуется внутри ячейки». Кандидат по коду: критерий показа кастома из волн B-20/B-23 (автонаследование, «вся связка == фасад») снимает показ и при ЧАСТИЧНОМ совпадении окна с фасадом. На старте: собрать точное репро (бой секции, бой ячейки) | S–M | показ = тому, что звучит | новая |
| 29 | B-31 | Редактор/анимации | Smooth snapped resize: граница между ячейками визуально принята 2026-08-31 (`chord ticks` и `1/та/и/та` frozen на стартовых координатах, edge-отступ счёта плавный, засечка под текущей границей мягко гаснет/возвращается; model commit один раз на `pointerup`). Правый край квадрата — сдан 2026-09-02 (ver 0.110): фиксированные px-треки (ячейки не резинятся), эталон заморожен на жесте, плавный доезд после отпускания, шаг превью бит-в-бит с соседним рядом | M | дорогая плавность resize, 60fps | **сдано 2026-09-02** (граница — 2026-08-31, правый край — ver 0.110) |
| 30 | B-32 | Транспорт/UI-анимации | Новая идея пользователя 2026-08-31: изменение темпа скроллингом сделать похожим на анимацию будильника на iPhone — ощущение вращения/инерции picker-колеса при изменении BPM | S–M | приятнее менять темп, тактильность UI | новая |
| 31 | B-33 | Редактор | Выделение ячеек и буфер обмена (идея пользователя 2026-09-01, дословно): «мне нужно чтобы ячейки можно было выделять, копировать, вставлять, вырезать». Охват: мультиселект ячеек (способ выделения — клик/драг/Shift — спросить на старте), Copy/Paste/Cut (+ горячие клавиши), вставка внутри квадрата и между квадратами/секциями с переносом аккорда, span, timesig и кастомного ритма (окно рулона). Детали взаимодействия — спека на старте волны | M | массовое редактирование прогрессий | новая |

Решения ask_user 2026-08-26 (B-06, прошиты выше): финал — **дематериализация** (ссылки/рулона нет, удар-в-начале возвращается); правило охватывает **любую** ячейку с пустым звучащим окном (бой был — тост по образцу выбивающихся волны-6); «бой не объявлен» = **только явный бой секции** (цитата про квадрат прошита в B-18); промежуточный показ призрачной тишины **прячем**.

Спека B-04 (дословно, 2026-08-25): «мне кажется, в режиме редактора засечки больше не нужны, 1 та и та и так справляются. давай уберем засечки и передвинем 1 та и та на их место. 1 нужно выделить (например сделать чуть чуть больше или жирнее)».

Спека B-15 (дословно, 2026-08-28; заменяет «переключаемую альтернативу в настройках» из пакета решений 2026-08-25): «Я придумал, это будет не включаемая опция, а подсказка для ресайза. По умолчанию всё должно выглядеть как сейчас. Когда пользователь начинает ресайз, кастомный ритм плавно „переплывает“ в место над счетом (каждый удар или струна на своем месте как в режиме ленты). Если ячейка наследует общий ритм секции, то ритм появляется над счетом через fade in. Когда пользователь отпускает мышь, ритм соответственно исчезает через fade out и уплывает на свое место.» Открытые точки на старте реализации: охватывает и бой, и перебор («удар или струна»); поведение при «+»/«−»-связках в момент показа; длительности/кривые анимации подберём в коде.

Спека B-15, уточнение (дословно, 2026-08-28): «а сделать так, чтобы стрелки или цифры кастомного боя именно переплывали, распределяясь по своим местам это возможно?» — принято: поударный FLIP (каждая стрелка/цифра летит из своего глифа мини-превью в свой узел сетки; источник j % unitLen, лесенка 10мс/удар, потолок 60мс). Урок реализации (вшит комментариями в код): все чтения раскладки — до рождения оверлея/одним проходом, иначе reflow запечатывает промежуточное состояние и переход схлопывается в 0→0 (первая версия B-15 легла ровно на это — поймано кадрами живой съёмки).

Решение 2026-08-29 по B-15/B-25: B-15 не сдаём и не добиваем точечно дальше, потому что баги выглядят как гонки DOM/рендера (stale mini-preview, полный render, clip в зуме). Переходим к B-25 как к системному фронту: сначала минимальный инкрементальный слой для редактора/ресайза, потом возвращаемся к B-15 и проверяем анимацию заново.

Итог B-15 2026-08-30: после B-25 и финальной полировки пользователь принял анимацию. Зафиксировано: target mini-preview не виден заранее, снятые ритмы fade-only, новые кастомные ячейки получают reverse flight, ghost клипуется по viewport при зуме, сброс зума по лупе анимирует ряды FLIP-transform без width-transition.

### B-31 handoff / текущий фронт (2026-09-01)

Пользователь оставил текущую волну в работе и начинает новую сессию. Продолжать **B-31: правый край квадрата**, не переключаться на B-32/BPM, пока явно не попросит.

Текущее состояние кода:
- HEAD перед этим handoff: `5f44814 Suppress edge resize hover ghosts`.
- Видимая версия приложения: `ver 0.98`; `sw.js`: `struchord-v98`.
- `bash dev/run-tests.sh` ранее проходил: `39 ok, 0 FAIL`; jsdom-тесты ловят регрессии, но **не доказывают визуальную приёмку** правого края.

Что уже принято пользователем:
- Обычный resize границы между ячейками принят. Нужная модель: smooth snapped resize, не guide-only и не rubber-stretch.
- Счёт `1/та/и/та` и `chord ticks` во время drag границы остаются на метрических местах; edge-отступ счёта плавный; засечка под текущей границей мягко скрывается и возвращается.
- 60fps guardrail обязателен: `pointermove` не должен делать полный `render()`, тяжёлый `innerHTML`, reslice/settle или запись модели. Допустимы rAF-throttle и редкая локальная работа только на snapped-переход.

Правый край квадрата — что пробовали:
- Базовый текущий эксперимент A: «выдвигание/задвигание». Добавление выглядело ближе к желаемому; удаление/задвигание всё ещё проблемное.
- Реализация сейчас использует `.square-edge-freeze-overlay`, `.square-edge-removed-strip`, `.is-removed-slide`, `.is-kept`, а также подавление hover/armed (`is-buttons-armed`) во время edge-resize.
- Эксперимент B «заслонка/шторка» (`b042b50 Try curtain square edge add animation`) пользователь отверг: «стало ещё хуже», откатить/не повторять тот же подход без новой идеи.
- Эксперимент A уже несколько раз улучшался, но **не принят**.

Актуальная открытая проблема (последний фокус):
- При shrink правого края, особенно `4 такта → 1 такт`, порядок кадров пользователя: `1` до resize, `2` до отпускания мыши, `3` после отпускания.
- На `ver 0.93` и затем при проверках пользователь видел справа серый/тёмный участок; на `ver 0.98` вопрос остаётся: «кадры 2 и 3 идентичны? сейчас при задвигании я вижу серое пятно».
- Не утверждать, что исправлено, пока это не подтверждено в браузере. Нужно самому снять/сравнить кадры именно на актуальной версии и смотреть **кадр 2 во время/после визуального settle до pointerup** против **кадра 3 после pointerup**.
- Подозрения: наложение overlay/gap/border/hover/armed/removed-strip. Ранее уже были ложные фиксы: дельта сдвига strip, duplicate boundary, hover ghosts, снятие overlay после settle — каждый частично менял симптомы, но пользователь всё ещё видит проблему.

Практический план для следующей сессии:
1. Сначала убедиться, что preview показывает актуальную шапку (`ver 0.98` или новую после правки), иначе это кэш/старый файл.
2. Самому воспроизвести `4→1` правым краем в реальном browser preview/headless Chromium; сделать кадры только потому, что это визуальная отладка и пользователь прямо просил тщательнее тестировать.
3. Сравнить: на кадре 2 не должно быть серого хвоста справа, а после visual settle до `pointerup` вид должен совпадать с финалом.
4. Править минимально: не ломать принятую границу между ячейками и не трогать B-15 rhythm-hint.
5. После правки — `node dev/tests/b31-live-boundary-resize.js`, `node dev/tests/b15-resize-hint.js`, `node dev/tests/b25-incremental-render.js`; полный `bash dev/run-tests.sh` перед push.

B-25, первый срез 2026-08-29: финал cell-boundary resize больше не вызывает полный `requestRender()`; вместо этого после ghost-анимации B-15 выполняется `syncResizedSquareDom(sec, sq, bi)` — локально обновляет grid/counts/ticks, mini-preview, кнопку `ритм`, cache rects, degrees и timeline. Цель: убрать гонку полного DOM rebuild с reverse-анимацией и stale preview.

B-25, второй срез 2026-08-29: операции без изменения числа ячеек (`changeChordSpanDirect`, `setEventTimeSig`) переведены на локальную синхронизацию квадрата через `syncSquareDomNow()`: обновляются ширины квадратов секции, grid/counts/ticks, бейдж тактов, title/text кнопки размера и кнопка `ритм`; полный `requestRender()` не вызывается, если DOM текущего квадрата найден.

B-25, третий срез 2026-08-29: структурные операции внутри квадрата (`addChordAfter` / `removeChordAt`) больше не пересобирают всю песню. Введён локальный rerender одного `.square-inner` (`buildSquareInnerEventsHtml` + `rerenderSquareInnerDom`), после которого переинициализируются previews, chord click, resize handles и square resize handle; проверяется актуальность `data-ei` и числа ручек.

B-25, четвёртый срез 2026-08-29: операции со списком квадратов секции (`addSquare`, `removeSquare`, `cloneLastSquare`) переведены на локальный rerender `.squares-list` одной секции (`buildSectionSquaresListHtml` + `rerenderSectionSquaresDom`), с сохранением scroll-фракции, edge-классов, repeat-рядов и переинициализацией handlers/previews. `setSquareCustomBeats` также закрывается локальным rerender текущего square-inner; `setSquareBeats` больше не дублирует полный render.

B-25, пятый срез 2026-08-29 (60fps guardrail): живой drag границы ячейки больше не выполняет тяжёлый `writeSpans()` на каждый `pointermove`; события схлопываются через `requestAnimationFrame`, последний `clientX` flush'ится на `pointerup`, а одинаковые snap-позиции вообще не пишутся. В тестах зафиксировано: пачка pointermove до rAF не делает DOM/model-записей и даёт не больше одной записи за кадр.

B-25, шестой срез 2026-08-29: ритм-модалка переведена на локальные обновления. Save/reset ритма ячейки синхронизирует текущий квадрат (`syncSquareDomNow`), save/reset ритма секции обновляет бейдж секции и локально пересобирает `squares-list` этой секции (`syncSectionRhythmDom`); полный `requestRender()` не вызывается при найденном DOM. Это закрывает ещё один источник stale mini-preview вокруг B-15.

B-25, седьмой срез 2026-08-29: простые свойства без изменения структуры переведены на точечные DOM-обновления: `setSquareRepeat` меняет только бейдж квадрата (`syncSquareRepeatDom`), `setSectionRepeat` локально пересобирает `squares-list` секции, `setSectionBpm` обновляет только BPM-бейдж секции (`syncSectionBpmBadge`) и таймлайн. В тестах зафиксировано отсутствие полного `requestRender()`.

B-25, восьмой срез 2026-08-29: секционный header обновляется локально через `syncSectionHeaderDom`: `renameSection`, `changeSectionType`, `setSectionKey` больше не требуют полного render; `setSectionTimeSig` мигрирует квадраты и затем локально обновляет header + `squares-list` секции. Убраны лишние `requestRender()` из popover модуляции после `setSectionKey`.

B-25, девятый срез 2026-08-29: правки аккорда из ручного ввода (`saveCurrentChord`), колеса (`selectChord`) и переименования из редактора аппликатуры синхронизируют только затронутую ячейку через `syncEventChordDom`; ступень вынесена в `syncDegreeHintForWrapper`. При смене auto-key обновляются headers/degrees без полного rebuild. В тестах зафиксировано отсутствие полного `requestRender()` для manual/wheel chord edit.

### B-31: диагноз «серого пятна» правого края и фикс (2026-09-01, ver 0.99)

Корень найден измерением, а не перебором overlay/gap/border: покадровая геометрия (зонд `dev/tools/probe-b31-edge-geom.js`, rAF-сэмплер) показала, что freeze-лента всегда приклеена к движущемуся краю (макс. зазор 1px = штатный gap сетки) — подозрения handoff не подтвердились.

Настоящая причина: **preview правого края считал проценты ширины от ЗВУКОВОГО эталона секции** (`getSectionMaxBeats`), а финальный рендер — от ВИЗУАЛЬНОГО (`getSectionMaxVisualBeats`). В секциях с ячейками своего размера эталоны расходятся: Wind of Change секция#4 — sound=20 / visual=18 (проверено по `uploads`); синтетика: 3/4-ячейки — 32/24, 2/4-ячейки — 32/16. Следствия обоих направлений:

- soundMax > visualMax: сетка сжата в слишком узкой коробке, на pointerup ширина скачет наружу (замер — до 93.7px);
- soundMax < visualMax: коробка шире px-сетки — при задвигании видна голая серая дорожка фона `.square-inner` (#D0CCC2) у правого края, исчезающая после pointerup.

Это ровно «кадр 2 ≠ кадр 3, при задвигании серое пятно».

Фикс (минимальный, `attachSquareResizeHandlers`): `previewWidthMetrics(eventsCopy)` считает и процент, и px-цель от ВИЗУАЛЬНОЙ длины preview-событий по визуальному эталону — та же формула, что у финального рендера; ширина ряда выводится из честного текущего рендера (`startVisual/visualMax`). Звуковой `maxBeats` остаётся потолком жеста, `setSquareCustomBeats` по-прежнему принимает звуковые доли.

Проверки: репро до/после в headless Chromium — оба направления перекоса дают кадр2 == кадр3 по ширине/сетке, скачок 0.0px, серых колонок нет (пиксельный скан); `dev/tests/b31-live-boundary-resize.js` + новый блок B-31.7; `b15-resize-hint.js`, `b25-incremental-render.js`; полный `bash dev/run-tests.sh` — 39 ok / 0 FAIL. Видимая версия `ver 0.99`, sw `struchord-v99`.

Статус: ждёт визуальной приёмки пользователем в live preview (критерий: задвигание 4→1 в секции с ячейками своего размера — кадр 2 до отпускания совпадает с кадром 3 после).

### B-31, продолжение (2026-09-01, ver 0.100): живой эталон секции

Первый фикс не закрыл случай пользователя: в Wind of Change секция 4 — ОДИН квадрат-гигант (5 тактов, звук 20 / визуал 18), т.е. квадрат САМ задавал эталон секции. При задвигании замороженный на старте `visualMax=18` diverged с после-коммитным `16`: кадр 2 = 22.22% (166.7px), кадр 3 = 25% (187.5px) — скачок +21px на pointerup (замер на живой песне, зонд `dev/tools/probe-b31-woc.js`).

Фикс: `previewWidthMetrics` считает процент и px-цель от ПОСЛЕ-КОММИТНОГО эталона — `max(пол 4 такта, самый длинный СОСЕД, визуал preview самого квадрата)`. Если эталон изменился, `syncSiblingsForSectionMax` на snapped-переходе ставит соседей, их controls и repeat-ряд на финальные ширины (с тем же width-transition 0.16s, класс `.squares-list.is-square-edge-resizing`), а бейдж тактов жертвы обновляется сразу, не на pointerup. Запись модели по-прежнему один раз на отпускании; sync — только style-свойства и только при реальном изменении эталона.

Проверки: живая песня Wind of Change — кадр 2 == кадр 3 (25% / 187.5px / «1 такт»); синтетика «жертва-гигант + сосед» — сосед при удержании уже на 100%, финал == удержание; `b31-live-boundary-resize.js` + блок B-31.8; полный прогон 39 ok / 0 FAIL. Видимая версия `ver 0.100`, sw `struchord-v100`.

### B-31, итог дня (2026-09-01, ver 0.102–0.106)

- **0.102** (`750eacf`): repeat-ряд обновляется на каждом snapped-переходе (`syncRepeatRowWidth`), из `previewWidthMetrics` убраны клэмпы в звуковых единицах (резали px-сетку → серый хвост).
- **0.103** (`c5ddffc`): removed-strip докрывает до правой кромки коробки — при быстром непрерывном драге просвет ≤1px (было 12px).
- **0.104** (`58e17d5`): эксперимент «follow_mouse» по решению ask_user (край непрерывно следует за мышью + мягкий доезд на pointerup, ghost-зона, right-anchored strip). Пользователь НЕ принял: «проблема не решена, добавились новые графические баги» (в т.ч. лишняя вертикальная линия у края — border-left ghost-зоны). **Откачен целиком** revert'ом.
- **0.105** (`b16e9ab`): поведение = 0.103 (плавные snapped-переходы, после-коммитный эталон, соседи/repeat-ряд на снапах). Версия поднята вверх (не вниз), чтобы SW-кэш гарантированно обновился.
- **Решение пользователя по сути правила макета (2026-09-01): залипание края на 100%, пока жертва сама эталон, и ступени 75→50→25% ниже пола 4 тактов — ЗАДУМАННОЕ поведение**, его не трогать. Следование края за мышью между снапами отвергнуто.
- **0.106**: закрыт отложенный пробел — `renderEventStrumPreviews` вызывается в `renderSquareEdgePreview` (превью боя больше не «хлопает» появлением на pointerup).
- **0.107** (instant cells: клонов нет, клетки встают разом, едет только край) — **НЕ принят пользователем**: «то, что ты сделал в 107 не решило проблему, только испортило хорошо работавшие анимации». Откачен целиком; **0.108 = код 0.106** (клоны, слайд-шторка, overlay на расширении — всё как в 0.106), версия поднята только ради SW-кэша. Урок: замеренные «76 кадров двойного изображения» либо не то, что видел пользователь, либо не главное; следующие шаги по B-31 — только от заметок 1–2 (заморозка превью ритма и эталона), не от моих инструментальных гипотез.
- **0.109** — исполнение заметок 1–2 (диагноз по кадрам пользователя + покадровый замер): превью/ячейки «резинились», потому что px-треки preview были УПРУГИМИ (`minmax(0,Xpx)` сжимались/растягивались вместе с анимирующейся коробкой — замер: при расширении Police ячейка 316.8→372 пока коробка 637→750; кадр 2 пользователя: шаг 109.6px при финальном 113.3px). Фикс: **фиксированные треки** `repeat(K, Xpx)` — ячейки встают мгновенно, коробка доезжает и открывает/режет их краем. Заметка 2: live-sync соседей/repeat удалён (заморожены на стартовых ширинах весь жест), после pointerup — плавный FLIP-доезд ширин/controls (settleSquareEdgeAfterCommit, работает на процентах, не на ректах). Проверено на живых песнях: ячейки неподвижны весь ход (Police), сосед/repeat заморожены (WoC), пиксельных голых прогонов 0, 59fps, jsdom 39 ok. Клоны/слайд-шторка 0.106 НЕ тронуты.
- **0.110** — «непропорциональность и расхождение» (принято 0.109, остаток по просьбе пользователя): базис процентов считался от КОНТЕНТ-ширины жертвы (box−2px границ), а границы не масштабируются — на сцене «resize test» (4→64) это −26px, шаг превью 43px при финальных 44.9px. Фикс: базис на уровне BOX (`basisBoxWidth = startBox × visualMax/startVisual`), границы вычитаются ПОСЛЕ масштабирования (`targetContentWidth = targetBox − borders`). Замер на сцене пользователя: ячейка превью на КАЖДОМ снапе (16/28/44/56/64 кол) = 44.88px = соседний ряд = финал, diff 0.0; в полёте неподвижна. 39 ok.
- **0.111** — доработка по замечанию пользователя («эталон перестраивается мгновенно и квадрат снова становится у правого края экрана»): заметка №2 реализована ДОКОНЦА — процент самой жертвы тоже считается от СТАРТОВОГО эталона (visualMaxBeats на pointerdown), а не от живого futureVisualMax. Пока жертва сама эталон, живой знаменатель держал её на 100% (край у правой кромки весь жест). Теперь: край отъезжает влево на каждом удалённом такте (замер: 64→56→48→20 долей = 100→87.5→75→31.25% от стартового 64, сосед заморожен 6.25%), после отпускания — плавный доезд к настоящему эталону (31.25→100%, край 576→1092 анимированно). B-31.7 не меняется (эталон держит сосед), B-31.8: hold 12.5% (3/24 стартовый) → финал 18.75%. 39 ok.
- **0.112** — «серая заливка» при удалении такта (кадры пользователя, уточнено ask_user: вся ячейка удаляемого такта, цвет #d0ccc2 = голое тело квадрата, всю анимацию; при добавлении — отлично). В моём окружении НЕ воспроизводится (3 песни, зум 1/1.5, быстрый/медленный драг, headless shell/new — шторка и клоны видны, серых прогонов 0), поэтому два хода: (1) ГАРАНТИЯ — removed-strip сам несёт фон ячейки (--color-surface + panel-sheen): даже если клоны не отрисовались, зона выглядит ячейкой, а не серой дырой; (2) убраны will-change:width (sqInner) и will-change:transform (strip) — форсированные композитные слои в связке с contain:layout-style/paint — главный кандидат на GPU-зависимую невидимость overlay-содержимого только у пользователя. Кадры из ветки (`ffa6d80`) расшифрованы: зона удаляемого такта — сплошной прямоугольник warm-серого ~231px (macOS-профиль сдвигает #d0ccc2 в (197,204,208)); в кадре добавления такого блока нет. Подтверждено: у пользователя шторка/клоны не видны (GPU/композитинг-зависимое), у меня — видны. 0.112-гарантия превращает зону в белую ячейку независимо от причины; will-change-слои сняты как главный кандидат. Если зона станет белой, но ПУСТОЙ (без имени аккорда) — клоны всё ещё не рисуются, копать глубже (DOM-зонд на машине пользователя).

**0.114** — найдена и устранена вероятная ПРИЧИНА серого (пользователь: «мне нужно починить сплошной серый прямоугольник»): settleSquareEdgeFreezeOverlay снимал overlay по 320мс-fallback'у, а на нагруженной машине (в частности под pinch-зумом) width-переход стартует позже — overlay исчезал до того, как край доехал, и открытое тело .square-inner (#d0ccc2) было видно «всю анимацию» сплошным прямоугольником. В headless переход стартует мгновенно, поэтому у меня не воспроизводилось. Фикс: авто-снятие overlay по таймеру УДАЛЕНО вообще — overlay живёт до следующего снапа (пересборка) или pointerup (cleanup); hold ≠ финал — штатная семантика 0.111, отложенная ветка коммита (squareEdgeAfterFreeze) не нужна, коммит на pointerup прямой. Замер: HOLD +560мс с зажатой мышью — шторка ещё на месте (раньше уже снесена), после pointerup чисто; серых прогонов 0; 39 ok. Вместе с 0.112 (белый фон шторки как гарантия) серому неоткуда взяться.
**0.115** — пользователь: «серое пятно не исчезло» (проверка под ручным pinch-зумом; синтетический pinch-драг в headless невозможен — hit-testing эмулированных событий под pageScaleFactor не работает, ограничение среды). Финальная гарантия: `.square-inner.is-square-edge-resizing { background: var(--color-surface) }` — тело квадрата на время жеста красится в цвет ячеек; любой оголившийся участок (хвост сетки, пустые треки, зона шторы при GPU-сюрпризах с её слоями) выглядит ячейкой, а не #d0ccc2-дырой. Разделители оставшихся тактов на жесте рисуют overlay-границы; после pointerup класс снимается и возвращается штатный фон зазоров. Открытый вопрос к пользователю: какая версия в шапке была при проверке (SW-кэш мог держать старую). — Ответ: 0.114 + Ctrl+Shift+R, серое сменилось БЕЛЫМ, содержимого удаляемого такта не видно; при добавлении не отрисовываются границы крайней ячейки до pointerup. Пользователь закоммитил видео uploads/Запись экрана 2026-09-02 в 21.51.21.mov (60fps, 2000×534, DPR≈1.5 — pinch-зум).

**0.116 — расшифровка видео, решающая находка**: покадровый анализ (552 кадра) показал, что в момент снапа (f474) пиксели пропадают и у СОСЕДНИХ квадратов: r2 мигнул и восстановился за ~100мс, r3 опустел ДО КОНЦА записи — DOM-логика соседей в жесте не участвует (frozen 0.109), значит это сброс растровых слоёв композитора Chrome (macOS, дробный DPR 1.5 после pinch — известное семейство багов raster/contain). Это объясняет ВСЕ симптомы разом: серый #d0ccc2 (0.111, тело сквозь дыру), белое пятно без содержимого (0.115, тело перекрашено), пропавшие границы на добавлении. Клоны не «не создаются» — у пользователя НЕ РАСТРИРУЮТСЯ слои. Фикс: удалён contain (layout style / layout style paint) из всей машинерии resize: .square-inner.is-square-edge-resizing, .square-edge-freeze-overlay, .square-inner.is-live-resizing, .resize-metric-overlay. Перф-цель contain здесь была мизерной (width коробки всё равно лэйаутит страницу). В моём headless репро невозможно (SwiftShader). Проверка у пользователя: жест БЕЗ пинча и ПОД пинчем — разделить факторы. 39 ok.

**0.117 — диагностическая сборка + layer-nudge**: ответ пользователя — «по удалению квадратов изменений нет, всё как в видео» (contain не был причиной). Видео переинтерпретировано: жест — УДАЛЕНИЕ при залипании 100% (оставшиеся ячейки растут, текст уезжает вправо), пустота держится до pointerup. DOM в hold в headless здоров (имена на месте) → разделяем «DOM пуст» vs «слои не рисуются» на машине пользователя: (а) HUD #b31-diag-hud (чёрный, слева внизу) — при каждом снапе «snap · N/M» (N = ячеек с именем в DOM) + console.log «B31-DIAG …», убирается на pointerup; (б) layer-nudge — translateZ(0) на два кадра на .section-card при снапе: принудительная перерастеризация (лечит залипшие растры на дробном DPR, иначе безвредно). Инструкция: жест удаления → в момент пустоты прочитать HUD: N≈M при пустом экране = композитор; N≪M = DOM. 39 ok.

**0.118 — HUD v2 по результату пользователя: «snap · 0/4»** — DOM ПУСТ на машине пользователя (не композитор!). При этом в headless тот же путь даёт 7/7, buildPreviewEventsForBeats аккорды сохраняет, convertSpanBetweenTimeSigs NaN-безопасен. Нужно понять, НА КАКОМ этапе у пользователя уходят аккорды: HUD v2 показывает beats, val0 (живой input.value первой ячейки), dispLen (длина innerHTML дисплея); в console.log — полный дамп: start (снимок событий на pointerdown: chord|span|timeSig), preview (eventsCopy на снапе), parentTs. Разбор: start пустой → снимок сломан; preview пустой → buildPreviewEventsForBeats; оба полны, DOM 0 → innerHTML-рендер/fit. Попутно исправлен самозвнос: вызов b31SnapDiag стоял ДО const eventsCopy (TDZ ReferenceError ронял первый же снап). 39 ok.

**0.119 — пользователь по 0.118: «по-моему, отлично» — содержимое видно всю анимацию!** Остаток: «во время анимации пропадают границы некоторых ячеек». Причина найдена сразу: защита 0.115 (тело квадрата → белый на время жеста) делала тёплые межячеечные зазоры — они и есть визуальные границы карточек — белыми-по-белому; этим же объяснялась ранняя заметка «при добавлении не отрисовываются границы крайней ячейки до pointerup». Фикс: background:surface СНЯТ с .is-square-edge-resizing — телу возвращён штатный тёплый фон зазоров на весь жест (зона удаляемого такта всё равно закрыта белой шторкой 0.112 с собственным фоном). Гипотеза по загадке «snap · 0/4» на 0.117: тест, вероятно, шёл на песне с пустыми ячейками (напр. «resize test» — все chord='', рендер пуст корректен); уточнить у пользователя. HUD оставлен до финального подтверждения (снять в 0.120). 39 ok.

**0.120 — «серое пятно вернулось» (по 0.119) → приклейка шторки геометрией.** Снятие белой заливки (0.119) открыло дыру, которую та маскировала: правый хвост. Правый край шторки обязан совпадать с сужающимся краем коробки весь полёт — раньше это держалось на идеальной синхронности ДВУХ CSS-переходов (width коробки + transform шторки, оба 0.16s); на машине пользователя они расходились — между краем шторки и краем коробки проглядывало тёплое тело (#d0ccc2). Фикс: `.square-edge-removed-strip { padding-right: 4096px }` — фон шторки продлён далеко за движущийся край и режется overflow:hidden overlay-а ровно по краю коробки в каждом кадре, независимо от таймингов; абсолютные дети (клоны) от padding не сдвигаются, transform по-прежнему везёт содержимое (одобренный слайд сохранён). Замер: strip.right = box.right + 4096 весь полёт; пиксельно в жесте — только легитимная статичная тёплая полоса рисунка боя в нижней части ячеек (есть и в покое, #d0ccc2, y≈65 — не дыра). 39 ok. HUD ещё на месте.

**0.121 — B-31 ЗАКРЫТ**: подтверждение пользователя по 0.120 — «Супер, ты молодец» (серого нет, содержимое видно, границы целы). Убрана вся диагностическая обвязка 0.117–0.118: HUD #b31-diag-hud, console-дамп B31-DIAG, layer-nudge (translateZ(0) при снапе). Итоговая формула B-31: эталон заморожен на жесте (0.111) + шторка с собственным фоном (0.112) + приклейка к краю геометрией padding-right (0.120). Хвост «snap · 0/4» ЗАКРЫТ: пользователь подтвердил — песня была с пустыми ячейками; показание честное (рендер пуст корректен), аномалии не было. Далее: B-32 (BPM-скроллинг как будильник iPhone). 39 ok.
- Статус волны: **сдано 2026-09-02** (ver 0.110 — «наконец то готово»; 0.111 — доработка заметки №2; 0.112 — серая заливка удаления, у пользователя на проверке).

### B-31: остатки на приёмку (заметки пользователя 2026-09-01, дословно)

Распределение по волнам (решил ассистент): заметки 1–2 — текущая волна B-31 (правки 0.108+); заметка 3 — отдельная волна B-32 (уже в backlog, подтверждена повторно).

**1. Превью ритма не резинится** (текущая волна B-31):
«во время ресайза, превью ритм не должен стягиваться и растягиваться, должен оставаться на своих местах(над 1 та и та)».
Техническая привязка: превью боя/перебора сейчас живёт в потоке ячейки и едет вместе с её шириной; нужно держать его на метрических позициях над счётом (по образцу frozen-счёта из B-31: абсолютный слой, координаты по долям, не по px-колонкам).

**2. Эталон не обновляется на ходу + превью не дергается** (текущая волна B-31):
«при удалении тактов за правый край, если тактов больше 4, эталон 100% не должен обновляться на ходу, только после отпускания мыши квадрат должен плавно принимать свою форму. во время анимаций с превью ритма не должно ничего происходить(сейчас я вижу что во время анимации, превью дергается)».
Техническая привязка: сейчас `previewWidthMetrics` + `syncSiblingsForSectionMax`/`syncRepeatRowWidth` переставляют эталон на каждом снапе (фикс 0.100). Требование: на время жеста эталон заморожен на стартовом, финальные ширины — плавным переходом после pointerup. «Дергание» превью, вероятно, следствие 0.106 (`renderEventStrumPreviews` теперь вызывается в `renderSquareEdgePreview` на каждом снапе — превью пересоздаётся в середине анимации); решать вместе с заметкой 1 — превью вообще не должно перестраиваться во время width-анимаций.

**3. BPM-скроллинг как будильник на iPhone** (волна B-32, backlog #30):
«пусть изменение темпа скроллингом будет похоже на анимацию будильника на айфоне.»
Подтверждено повторно 2026-09-01 — идея жива, кандидат на следующую волну после закрытия B-31.

**0.122 — найдена и исправлена ошибка единиц в формулах 0.113** (пользователь: «тултип всё ещё не на месте»). `visualViewport.offsetLeft/Top` по спецификации УЖЕ в CSS-px — а 0.113 делил их на scale (визуальные метрики: visLeft=offsetLeft/scale). При пане после пинча (замер: offsetLeft=400 при scale 1.5) видимый регион съезжал влево на 133px → тултип прижимался к чужой границе. То же в mouseEventToLayoutXY: было (client+offset)/scale, стало client/scale+offset. Верификация против CDP-эталона (Page.getLayoutMetrics): метрики совпадают точно (visLeft 400, visW 533.33), mouse (400,300visual) → (666.7, 200) ✓, при scale=1 бит-в-бит прежнее. Финальная приёмка — реальный жест у пользователя. 39 ok.

**0.123 — по видео 01.20.22 (новый визуальный баг, pinch-зум ~2.3×):** границы ячеек В СЕРЕДИНЕ квадрата скачут на снапе жеста и возвращаются на pointerup (щели мигают белым/тёплым). Причина: hold-сетка — фиксированные px-треки от targetContentWidth (механика 0.110, ячейки не резинятся в полёте), финал — fr от фактической percent-ширины; округление процента даёт ±1 CSS px — на DPR2×pinch это видимые 5px. На scale=1 расхождение нулевое (замер: hold==финал до сотых), потому не ловилось. Фикс: по transitionend width переводить колонки в тот же fr-вид, что финал (тег против гонки со следующим снапом) — settled-hold == финал при любом масштабе; в полёте px сохранены. Замер после фикса: HOLD grid repeat(8,minmax(0,1fr)), координаты ячеек hold==up 343/530.5/624.25 бит-в-бит. 39 ok.

**По видео 01.14.24 (тултип, 0.122)**: триангуляция с реальным pan подтвердила — fixed-координаты и контент в одном пространстве, формулы 0.122 геометрически верны (маркер по rect ячейки встал на экран ровно в ожидаемое место). Расшифровка записи: тултип при сильном зуме уходит вбок (слева, впритык к ячейке) — это штатная ветка дизайна 0.113 «вертикаль невозможна → вбок», не единичная ошибка. Открыт вопрос пользователю о желаемом поведении при сильном зуме. — Ответ (2026-09-03): «пусть он находится в том же месте, как и без зума (ничего страшного, если не будет помещаться на экране)».

**0.124 — тултип при зуме = позиция как без зума**: tooltipViewportMetrics всегда возвращает окно раскладки (window.innerWidth/innerHeight) — visualViewport больше не участвует в позиционировании; якорь ячейка, вертикальный приоритет и клэмпы — те же, что при scale=1. Попадание в видимую область при pinch НЕ требуется (решение пользователя). mouseEventToLayoutXY (хиттест вооружения кнопок) сохранён. Замер: под pinch 1.5 + пан позиция бит-в-бит равна scale=1 (tip 305.8/375.2, центр по ячейке, отступ 12). 39 ok.

### Tooltip аппликатуры при pinch-зуме (2026-09-02, ver 0.113)

Заметка пользователя: «когда наводишь мышь на ячейку с аккордом, тултип аппликатуры появляется ровно где нужно; но стоит увеличить страницу жестом двумя пальцами — тултип появляется вообще не там, как будто в рандомном месте».

Диагноз (CDP Emulation.setPageScaleFactor = pinch): при visual-viewport зуме `window.innerWidth/innerHeight` остаются layout-размерами (у меня 1200x800 при видимом 750x500), а тултип с `position:fixed` позиционируется/clампится по ним. Плюс на реальных жестах clientX/Y приходят в visual-px, а кэш rects — layout-px.

Фикс 0.113:
- `tooltipViewportMetrics()`: видимый регион в layout-px через visualViewport (offset/scale, size/scale); при scale===1 вырождается в прежние innerW/H — поведение бит-в-бит;
- `positionMainTooltip`/`positionPreviewTooltip`: свободное место, fitsVertically, fallback-центр и все прижатия — от видимого региона;
- `mouseEventToLayoutXY(e)`: нормализация clientX/Y в layout-пространство для хиттеста вооружения кнопок (при scale===1 — тождественно).

Проверено: scale 1.0 — без регрессий (якорь dx=21 dy=-4, тултип в регионе), синтетический pinch в headless ограничен (CDP-события идут в layout-px, :hover при синтетике ненадёжен) — финальная приёмка на реальном жесте у пользователя.

### Отменённые (R4 — не стираем, причина прилагается)

| ID | Зона | Что было | Статус |
|----|------|----------|--------|
| А-04 | Аппликатуры/UI | Бейдж anchor в каталоге | отменено 2026-08-25 — линия аппликатур закрыта («пока мне все нравится») |
| А-05 | Аппликатуры/UI | Цифры пальцев на грифе | отменено 2026-08-25 — та же причина |
| А-06 | Аппликатуры | below-barre `5,2,2,2,2,0` (Am7) | отменено 2026-08-25 — та же причина |
| А-07 | Аппликатуры | Ножницы/обрыв на 3-м ладу (A7sus4) | отменено 2026-08-25 — та же причина |
| А-08 | Аппликатуры | q39: порог «скачки высоты» | отменено 2026-08-25 — та же причина |
| А-09 | Аппликатуры | q42: расхождение `x,12,11,9,12,0` (Aadd9) | отменено 2026-08-25 — та же причина |
| А-10 | Аппликатуры | Водораздел тяжёлых caged | отменено 2026-08-25 — та же причина |

Перенумерация 2026-08-26 (решение ask_user): ряд был B-04…B-10 и конфликтовал с активными/сданными теми же номерами — аппликатурной линии присвоен префикс «А-». README на эти пункты ссылается по именам (q39/q42 и т.п.), не по ID, поэтому правка только здесь.

Если аппликатурная тема всплывёт снова — вернём нужный пункт в активные ПЕРЕВЫПУСКОМ под свободным B-номером (старые А-номера остаются архивом), контекст в README не потерян.

## Реестр волн в проде — ритм-линия «Рулон и окна»

Полные sha фиксируются с чистки-1; для ранних волн в сводках сохранились префиксы.

| Волна | Дата | sha256 STRUCHORD.html | Размер | sw |
|-------|------|------------------------|--------|----|
| Ритм в-3 (лента связок позиционно) | — | `62eb4f…` (префикс) | — | — |
| Ритм в-4 (кэш изъят, пул в сейвах) | — | `88659109…` (префикс) | — | — |
| PWA-упаковка | 2026-08-24 | `66a7299c…` (префикс) | — | — |
| Ритм в-5 (лента пары, проекция наследника) | — | `d3b5535e…` (префикс) | — | — |
| Чистка-1 | 2026-08-25 | `445caca1f415c7e383e66acce8344c1f1651c1a22098c4635196bc7a3b791c5b` | 1 438 803 | — |
| Ритм в-6 (каноника сетки, лента по цепочке) | 2026-08-25 | `c175a1747fa2994fa317145f1404cb6cf96cfa7ba35277af543a28d81e481da4` | 1 446 956 | v4 |
| Ритм в-7 (автонаследование) | 2026-08-25 | `afac59ca490705324a509df27c90f546903bfbe091f3a1c9d38bba3fc9d81113` | 1 453 909 | v5 |
| В-7 уточнение фасада (**в проде**) | 2026-08-25 | `7fdd8041d1f7b7008ebbf850c399f971ae7b3b07a6bfb56802bf71e409ce0961` | 1 454 954 | v6 |
| B-01 (мобильные цели >= 40px) | 2026-08-25 | `dde15d637825324659f932ab6697e32d7922225d2bc913425eff9462cdff1dc9` | 1 455 958 | v7 |
| B-02 чистка-2, scope='square' | 2026-08-25 | `edafbe16e818894d7750d03957efb9cb927921047c6d6bbf9b6acdc0b6dba2d3` | 1 455 395 | v8 |
| B-03 лесенка sub8 | 2026-08-25 | `22fc7069144c30edcc3380a40861cb56439f8b088116e485750cd91ff404fc09` | 1 456 115 | v9 |
| B-04 засечки убраны | 2026-08-25 | `fab338f1e076300198f25369064b7163abfff2fa11119849224131ddb7d50b53` | 1 454 343 | v10 |
| B-04 уточнение: выделяется только «1», подписи +3px | 2026-08-25 | `be29a03aab12e073a8a126cfc178c126546931261ccaa3694622bcd7c48be3b9` | 1 454 380 | v11 |
| B-06 ресайз: пустой бой при необъявленном бое секции не материализуется | 2026-08-26 | `580d67d29e5ab9dbeb74517ec09028bac803e7ae0e41bfac744d48ff9779c522` | 1 463 132 | v12 |
| B-19 счётные подписи по эпохам размера | 2026-08-26 | `d23f6f8c204211d920647841658634457e9017d72b4b23abf12b453495cd1302` | 1 466 222 | v13 |
| B-21 подписи счёта выше, «1» выделяется | 2026-08-26 | `d5b7a09c56ed8ab2dcc1e6729514a70d436aa2b90eeadaafe8deea33f905c643` | 1 467 205 | v14 |
| B-22 низкие засечки под подписями | 2026-08-26 | `14badc5001a88568b118e1fd112774c19637b30e57f371f16220d838e6c06da8` | 1 471 618 | v15 |
| B-21 уточнение: «1» только больше, не жирнее | 2026-08-26 | `4e54e862f5ff176ac491b426611487a4dfa981efe441271bec890ec4c9360aff` | 1 471 760 | v16 |
| B-16 выбор схемы лонгпрессом на кнопке темы | 2026-08-26 | `d88e8353b0a32b5fda9d48360dd282e540c4ca3ae49b96089fef43662c5c4f03` | 1 480 275 | v17 |
| B-20 автонаследование на структурных операциях + «−» перенимает ленту | 2026-08-26 | `b133a3c75aebbc20fb939a3bc083db94fcdc5b7b29f93b10451b7a93d57aa8b3` | 1 485 792 | v18 |
| B-05 чистка-3: снос мёртвого правила `.is-count-tight` | 2026-08-27 | `d9fda43f9d8fc32670214def4e21d1d3b57a53fa82fa28afb34901577da86629` | 1 485 258 | v19 |
| B-23 «−» со стороны пиннатого поглотителя: полная запись звука (**в проде**) | 2026-08-27 | `6542b19bc5e99f535d78cedf82912018239431444d8c0f76fc82eae9f7a5e47e` | 1 488 920 | v20 |
| B-29 clearAll чистит пул кастомных ритмов | 2026-08-29 | `41f29be0e1f4e9897aa794f7576db04b7edec2d42d57ebc1df4a6358ecdc7987` | 1 558 354 | v49 |
| B-15 подсказка ритма при ресайзе (**в проде**) | 2026-08-30 | `86b6064e1f7701420e99483272a7aa56bd415941ec72da309f1334c3beefd8b0` | 1 563 226 | v55 |
| B-31 smooth snapped resize: правый край квадрата (**в проде**) | 2026-09-02 | `0b28f1bffc4ed6cfd34b1e7454f9c8322e889a678fc388d25e2b163d8b14e848` | 1 616 264 | v111 |

Аппликатурные волны (2–7, 2026-08-13/14) — детали и прогоны в README, здесь не дублируем (R1).

## Как вести этот файл

- Новая идея → строка в Backlog с ID вида `B-NN` (следующий свободный — B-33).
- ID уникален сквозным образом по ВСЕМ рядам документа (активные/сданные/отменённые) — переиспользование запрещено: ID нужен, чтобы сослаться позже (R4). Коллизия 2026-08-26 (аппликатурные B-04…B-10 vs ритм/редактор те же номера) исправлена: архиву присвоен префикс «А-» (решение ask_user 2026-08-26).
- Старт волны → статус «в работе»; сдача → «сдано» + строка в реестре (для ритм-линии — с sha/sw).
- Раз в несколько волн перечитываем порядок — он живой, не догма.
